# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.next
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# Environment variables (contains secrets — never commit)
.env
.env.*
# ...ยกเว้นไฟล์ตัวอย่างที่เป็น placeholder (ต้อง commit) — negation ต้องอยู่ท้ายสุด
!.env.example

# TypeScript incremental build cache
tsconfig.tsbuildinfo
