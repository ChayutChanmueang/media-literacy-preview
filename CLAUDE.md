# CLAUDE.md

คำแนะนำโปรเจกต์ฉบับเต็มอยู่ที่ [AGENT.md](./AGENT.md) — อ่านไฟล์นั้นก่อนเริ่มงานเสมอ (stack, สถาปัตยกรรมโค้ด, กติกา accessibility, วิธีจัดการเอกสาร agile/sprint, สถานะโดเมน)

ไม่ต้อง duplicate เนื้อหาไว้ที่นี่ — ถ้าจะเพิ่ม/แก้กติกาโครงการ ให้แก้ที่ `AGENT.md` ไฟล์เดียว เพื่อไม่ให้สองไฟล์ขัดกันเอง

## หมายเหตุเฉพาะ Claude Code

- โปรเจกต์นี้เป็นเอกสารภาษาไทยเป็นหลัก (docs, sprint, user stories) — ตอบและเขียนเอกสารใหม่เป็นภาษาไทยให้สอดคล้องกับของเดิม เว้นแต่ผู้ใช้ขอเป็นอย่างอื่น
- ก่อนแก้ `docs/agile/**` ให้ `ls`/`Glob` เช็คโครงสร้างไฟล์ปัจจุบันก่อนทุกครั้ง เพราะไฟล์ sprint ถูก rename/ควบรวมบ่อย (ดูกติกาข้อ 4 ใน AGENT.md)
